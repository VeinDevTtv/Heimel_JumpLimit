config = {
    maxJumps     = 2,    -- how many jumps you can do in a row
    jumpCooldown = 1000, -- ms between counted jumps
    resetTime    = 5000, -- ms after last jump before count resets
    ragdollTime  = 3000, -- ms that ragdoll lasts
    debug        = true, -- toggle debug messages
}
